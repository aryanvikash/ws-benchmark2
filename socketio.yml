config:
    target: 'http://localhost:8080'
    phases:
        - duration: 120
          arrivalRate: 10000000000

scenarios:
    - engine: 'socketio'
      flow:
          - emit:
                channel: 'message'
                data: 'Hello, world!'
