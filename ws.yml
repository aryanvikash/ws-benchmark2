config:
    target: 'ws://localhost:8080'
    phases:
        - duration: 120
          arrivalRate: 10000000000
    ws:
        maxRedirects: 25

scenarios:
    - engine: ws
      name: Echo a string
      flow:
          - send: 'Hello from Artillery'
